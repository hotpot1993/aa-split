# AA分账App — 开发进度存档

> 存档时间：2026-08-24 · 对应技术方案 §7 开发计划

## 一、一句话状态

**M0~M5 的代码已全部生成完毕并通过构建与单元测试**；M6（真机测试/商店上架）与「真实后端联调」未开始。

- 服务端：`npm install` ✅ · `npx prisma generate` ✅ · `npm run build` ✅（exit 0）· `npm test` ✅（**4 套件 / 31 用例全绿**）
- 客户端：`flutter pub get` ✅ · `flutter analyze` ✅（**0 issues**）· `flutter test` ✅（3/3）

## 二、里程碑状态

| 阶段 | 内容 | 状态 | 备注 |
|---|---|---|---|
| M0 搭建 | monorepo 初始化 / Docker Compose / CI / 设计令牌与字体 | ✅ 完成 | docker-compose、GitHub Actions、aa_design 令牌、ZCOOL KuaiLe + Long Cang（OFL）随包 |
| M1 账户 | 注册/登录/JWT/找回/改密 + 4 页 + 启动页 | ✅ 代码完成 | auth.service.spec 覆盖；登录限流 5 次/10 分钟 |
| M2 群组 | CRUD/邀请/成员/搜索 | ✅ 代码完成 | 邀请码 12 位唯一+冲突重试；软删除 |
| M3 记账 | 账单/四种分摊/参与人/凭证/详情 | ✅ 代码完成 | 分摊校验服务端事务内（sum=amount）；凭证 upload（local/minio 双模式） |
| M4 结算 | 最少转账算法/结算页/催款/已付/通知 | ✅ 代码完成 | 算法 13 例金标准单测（含已付份额排除）；SSE 服务端就绪 |
| M5 收尾 | 定期账单/统计/导出/设置与安全/关于 | ✅ 代码完成 | BullMQ 每日 03:00 扫描；xlsx/csv 导出 |
| M6 上线 | 打磨/真机测试/上架 | ❌ 未开始 | — |

## 三、交付物清单

### 根目录（M0）
- `README.md`（总览/快速开始/当前状态）· `.gitignore` · `.editorconfig`
- `docker-compose.yml`（postgres16 + redis7 + minio + api）
- `.env.example`（含 DATABASE_URL / REDIS / JWT / MINIO / SWAGGER 全部配置项）
- `Makefile`（server-*/app-*/infra-* 常用命令）
- `.github/workflows/ci.yml`（server：ci→generate→build→test→validate；app：pub get→analyze→test）

### 服务端 `server/`（NestJS 10 + Prisma 5 + PostgreSQL 16）
- 模块：auth / users / groups / bills / settlement / notifications / regular-bills / export / statistics / health / storage / prisma / common
- 全局：`/api/v1` 前缀、`{code,message,data}` 信封、错误映射（P2002→409/P2025→404）、JWT(30d/7d)+全局守卫+`@Public`、Throttler（全局100/min、登录5/10min）、分页与上限、Swagger `/api/docs`（SWAGGER_ENABLED）
- 数据：`prisma/schema.prisma`（9 表，金额整数分，§3.3 索引）+ `prisma/seed.ts`（演示数据）
- 单测：settlement.algorithm（13 例）/ settlement.service / auth.service / bills.util（共 31 例，全部 mock PrismaService，不连库）
- 交付：Dockerfile（node:20-alpine 多阶段）、.dockerignore、.env.example、README.md（运行步骤 + API 一览 + 简化点说明）

### 客户端 `app/`（Flutter + Riverpod + go_router + dio）
- **31 页全量**（P01~P60）：启动/登录/注册/忘记密码/重置/总览/账单列表/统计/账单详情/群组列表/创建群/邀请/群详情/成员/结算/催款/群设置/记一笔/分摊/选人/凭证/定期账单/消息中心/提醒设置/个人主页/设置/账号安全/导出/关于/搜索
- 架构：`core/`（config + api_client dio+JWT+统一响应）、`models/`（11 个模型）、`data/mock/`（团子酱 + 3 群演示数据）、`data/repositories/`（5 个仓库，Demo 优先）、`providers/`（Riverpod）、`router/app_router.dart`（go_router Shell 4 Tab + 中央 ➕ 凸起）
- `packages/aa_design`：SketchyBorder / PaperCard（胶带·图钉·微旋）/ StampBadge / HandAmount / DoodleButton / HandTextField / HighlightText / HandTag / HandToggle / ProgressPencil / EmptyState / ErrorState / TuanTuan（4 情绪）/ CheckDraw / StampDrop / PaperPlaneFly / CoinBurst / BellShake / 点阵纸 / 蜡笔柱状图 / 甜甜圈环形图 + 手写字体 asset
- 测试：`test/widget_test.dart`（主流程）、`test/aa_design_render_test.dart`

## 四、关键修复记录（质量事件）

1. **幽灵转账 bug**（结算正确性）：部分已付账单会把已付参与人再算成欠款 → `summarizeBalances` 增加 `paid` 排除 + 垫付人应收扣减已付份额；新增 2 例回归单测
2. **Prisma Client 过期**：schema 更新后未 regenerate → `BillInclude/BillGetPayload` 缺失、大量隐式 any → 重新 `prisma generate`
3. **@nestjs/bullmq v10 API 变更**：无 `@Process()` 装饰器 → 继承 `WorkerHost` 实现 `process(job)`
4. **MinIO putObject 参数错位**：mimetype 被传给 size 位 → 补 `buffer.length` + metadata
5. **regular-bills dto 导入路径**：少一层 `../` → 修正为 `../../bills/dto/create-bill.dto`
6. **测试符号约定**：assertValid 的净额方向写反（应收=正）→ 修正并保留零和校验

## 五、已知差异与边界（如实记录）

| 项 | 说明 |
|---|---|
| 客户端真实 API 分支 | ✅ **已接线**：5 个仓库 `useMock=false` 分支调用 `ApiClient`（dio+JWT+`{code,message,data}`）；token 持久化 SharedPreferences，启动页 `restore()` 恢复会话；analyze 0 issues / test 3/3 |
| 结算 billIds | **近似归属**（净额合并后按 from 应摊贪心归属到未结清账单），README 已注明 |
| GET settlement 额外字段 | 返回 `settlementIds`（与 transfers 对齐）供 `POST /settlements/:id/paid` |
| ratio 分摊 | 创建时按客户端算好的 shareAmountCents 走与 custom 相同的校验 |
| 定期账单副本 | payerId 简化为模板创建者 |
| SSE | 服务端 `GET /notifications/stream`（?access_token= 与 header 均可）；客户端消费未接 |
| 凭证拍照/导出/定时生成 | 客户端 P33/P34/P53 为模拟演示行为；真实凭证上传需接入相机后走 `BillRepository.addReceipt`（multipart） |
| 个人资料编辑（P50） | 服务端暂无 PATCH /auth/me，真实模式仅本地演示生效 |
| 忘记密码问题展示（P04） | 服务端无安全问题查询端点，真实模式下由用户自行输入 |
| 深色模式 | 同一套 token 换背景（`buildAaTheme(Brightness.dark)`），textTheme 已适配 |

## 六、下一步（按优先级）

1. **端到端自动化测试**：核心链路（注册→建群→记账→结算→催款→已付）——两端代码均已就绪
2. **联调跑通**：起 PostgreSQL → `npx prisma db push` → `npm run dev` → 客户端 `AA_USE_MOCK=false` 全链路验证（含结算、SSE、导出）
3. **真机阶段**：相机（P33）、推送（极光/厂商通道）、性能与弱网、商店上架（iOS $99/年）
4. **冲刺前**：生成 `prisma migrate` 正式迁移（替换 compose 里的 `db push`）

---
*存档 v1.1 · 关联文档：[技术方案](../AA分账App-技术方案.md) ｜ [产品原型](../AA分账App-产品原型.md) ｜ [UI 设计规范](../AA分账App-UI设计规范.md)*
